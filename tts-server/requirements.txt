fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
# torch / torchaudio / cosyvoice deps come from CosyVoice's own requirements.txt —
# install them after cloning the upstream repo:
#   pip install -r $COSYVOICE_DIR/requirements.txt
